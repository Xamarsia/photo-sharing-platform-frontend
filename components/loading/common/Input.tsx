import 'server-only';

export default function Input() {
    return (
        <div className="relative h-12 mb-4">
            <div className={` peer transition-all outline-none size-full 
                bg-gray-200 border border-gray-100 rounded-xl`} />
        </div>
    )
}
